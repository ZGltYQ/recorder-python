# Core Framework
PySide6>=6.6.0

# Audio Processing
sounddevice>=0.4.6
soundfile>=0.12.1
pulsectl>=23.5.0
pyaudio>=0.2.11

# Speech Recognition (Qwen3 ASR)
transformers>=4.36.0
torch>=2.1.0
torchaudio>=2.1.0
accelerate>=0.25.0

# Speech Recognition (faster-whisper backend, optional)
# Provides the "faster-whisper" option in Settings → Backend. Pulls in
# ctranslate2 (native wheels ~60 MB). Skip with --no-deps if you only
# want Qwen3-ASR.
faster-whisper>=1.1.0

# Speaker Diarization
resemblyzer>=0.1.1
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.11.0

# Database
SQLAlchemy>=2.0.0
alembic>=1.12.0

# AI Integration
httpx>=0.25.0
aiohttp>=3.9.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
appdirs>=1.4.4
pydub>=0.25.1
webrtcvad>=2.0.10

# Logging & Configuration
structlog>=23.2.0
pyyaml>=6.0.1

# Development (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.7.0
